# Strings that are used in the config map
# No idea if this has a future, but it works for now :)

IGNORE_DISABLED_CLIENTS = "ignore_disabled_clients"
AUDITOR_CONFIG = "auditors"
MONITOR_CONFIG = "monitors"
